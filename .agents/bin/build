#!/usr/bin/env bash
# Managed by shakacode/agent-workflows; this repo owns the command below.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
bin/yarn build
bin/rails tailwindcss:build
bin/shakapacker-precompile-hook
exec env RAILS_ENV=production bin/shakapacker "$@"
